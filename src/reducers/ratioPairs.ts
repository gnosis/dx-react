import { handleActions } from 'redux-actions'
import { RatioPairs } from 'types'

// import { setClosingPrice } from 'actions/ratioPairs'

// TODO: fill in when we have actions
export default handleActions<RatioPairs>(
  {
    // [setClosingPrice.toString()]: (state, action) => [...state, action.payload] as any,
  },
  [],
)
